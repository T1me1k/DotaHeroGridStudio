{
  "name": "dota-hero-grid-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node dev-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node build.mjs",
    "typecheck": "node --check src/core.mjs && node --check src/converter.mjs && node --check src/app.mjs",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
